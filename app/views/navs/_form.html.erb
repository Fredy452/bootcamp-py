<%= form_with(model: nav) do |form| %>
  <% if nav.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nav.errors.count, "error") %> prohibited this nav from being saved:</h2>

      <ul>
        <% nav.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :laber %>
    <%= form.text_field :laber %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
